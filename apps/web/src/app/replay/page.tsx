import { ReplayClient } from '@/components/replay/ReplayClient';

/** Local replay playback of the most recent match (recorded client-side). */
export default function ReplayPage() {
  return <ReplayClient />;
}
